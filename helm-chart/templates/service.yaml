apiVersion: v1
kind: Service
metadata:
  name: user-service
spec:
  type: {{ .values.service.type }}
  selector:
    app: user-service
  ports:
    - protocol: TCP
      port: {{ .values.service.port }}
      targetPort: 5000
      nodePort: {{ .values.service.nodePort }}